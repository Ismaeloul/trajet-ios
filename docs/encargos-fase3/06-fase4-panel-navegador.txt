Contexto: Trajet, servidor Python/FastAPI del usuario (repo C:\Users\Isma\Desktop\Trajet v2\trajet-server, rama rewrite-v2, ya con 619 tests en verde) con un panel de administración web «Cristal» en app/panel/ (HTML + panel.css + panel.js, sin CDN, CSP estricta). Documentación del proyecto en C:\Users\Isma\Desktop\Trajet v2\docs (openapi.yaml = contrato, diseno/sistema.md = sistema visual, decisiones.md, PARADAS.md). El encargo original está en C:\Users\Isma\Desktop\Trajet v2\PROMPT-trajet-v2.md, FASE 4.
Herramientas: Docker Desktop está disponible (`docker compose up --build -d` en trajet-server levanta el panel en http://127.0.0.1:7796, con TRAJET_ADMIN_PEERS=auto; lee docker-compose.yml y README.md). NO hay extensión de Chrome: usa el navegador integrado de esta sesión (herramientas mcp__Claude_Browser__*: navigate, computer con screenshot/click/type, read_page, find, form_input, read_console_messages, read_network_requests, resize_window) para PROBARLO DE VERDAD como un usuario, y la herramienta C:\Users\Isma\Desktop\Trajet v2\design-lab\tools\capturar.mjs (Chrome headless por CDP; lee su cabecera: `node capturar.mjs plan.json`, admite selector, eval, gif) para las capturas y GIF a tamaños exactos.
REGLAS DURAS: lanza SIEMPRE docker y cualquier comando con `env -u PRIM_API_KEY` delante y NUNCA imprimas, escribas ni pegues el valor de la variable de entorno PRIM_API_KEY (ni en el navegador). La prueba de la clave real por el panel ya está hecha en la FASE 1 (test real): aquí usa el PRIM falso o una clave inventada (ej. «CLAVEFALSA1234567890»), que el servidor rechazará y eso también es un caso a capturar. NO toques producción (192.168.1.188 / 100.99.38.76) ni el Umbrel ni publiques imágenes. Si aparece un login de Umbrel o de cualquier servicio, no metas credenciales: apúntalo como pendiente. No uses el navegador para nada ajeno al proyecto. No modifiques el contrato (docs/openapi.yaml) ni app/api/*: si encuentras un fallo de la API, apúntalo en el informe; sí puedes corregir el panel (app/panel/*) con cambios mínimos y sus tests (tests/test_panel.py, tests/test_admin.py). Al terminar, baja el contenedor (`docker compose down -v`) y borra la imagen `trajet:dev` si la creaste.

TU PARTE (FASE 4): PROBAR EL PANEL EN EL NAVEGADOR y dejar pruebas.
1. Levanta el panel con docker compose (BD vacía, sin clave PRIM: el panel debe avisar «servidor sin clave»). Comprueba que /api/health responde y que el panel carga en http://127.0.0.1:7796/ (si el panel da 403 por TRAJET_ADMIN_PEERS, lee README.md «Si algo falla» y usa el .env o la variable que indica, sin cambiar el compose del store).
2. Con el navegador integrado, a 390×844 (móvil) y a 1440×900 (PC), en claro y oscuro (resize_window admite colorScheme): recorre TODO el panel como usuario: cabecera y avisos; EMPAREJAR (generar QR: código y cuenta atrás de 5 min visibles; simula el iPhone canjeando el código con curl o fetch a POST /api/v1/pair y comprueba que el panel pasa a «Emparejado»; anular QR; QR caducado si puedes acelerarlo o al menos documentar); DISPOSITIVOS (renombrar, revocar con confirmación; comprueba con curl que el token revocado da 401); CLAVE PRIM (pegar una clave falsa → rechazada con motivo claro; comprobar que el campo se vacía y que NUNCA aparece una clave en el DOM ni en las respuestas de red: mira read_network_requests); CUOTA; OLLAMA; COLECTOR; SALUD; ERRORES; AJUSTES del QR (direcciones LAN/Tailscale: validación y que el QR nuevo las lleve). En cada paso: cero errores en consola (read_console_messages onlyErrors) y cero peticiones fallidas (read_network_requests: nada con estado ≥ 400 salvo los 4xx esperados de las pruebas negativas, que anotarás).
3. Capturas y GIF en C:\Users\Isma\Desktop\Trajet v2\docs\capturas\panel-fase4\ con capturar.mjs (y screenshots del navegador integrado donde aporten): móvil y PC, claro y oscuro, cada sección y cada estado probado; GIF de: generar el QR y la cuenta atrás, el paso a «Emparejado», revocar un dispositivo, y pegar/rechazar la clave. Ponles nombres claros y escribe un README.md en esa carpeta que diga qué es cada una.
4. Corrige en app/panel/* lo que falle (con tests) y, si algo de la API falla, apúntalo sin tocarla. Lanza la suite (`env -u PRIM_API_KEY .venv/Scripts/python -m pytest -p no:warnings`) al final: en verde.
5. Escribe C:\Users\Isma\Desktop\Trajet v2\docs\fase4-panel.md: qué probaste (tabla sección × tamaño × modo × resultado), errores de consola y peticiones fallidas (deben ser 0 salvo las esperadas), lo corregido, lo que no se pudo probar (login de Umbrel, PRIM real…) y las capturas/GIF.
6. Commit en trajet-server (solo si tocaste app/panel o tests; mensaje en español; `git -c user.name=Isma -c user.email=ismaeloulhaji@gmail.com commit`, termina con la línea «Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>») y push a rewrite-v2. Las capturas y fase4-panel.md van en el repo trajet-ios (C:\Users\Isma\Desktop\Trajet v2, commit con «[skip ci]» en el título, misma línea de coautoría, push a rewrite-v2).
Informe final: tabla resumen, errores encontrados y corregidos, ids de commits, lo pendiente.
