Lee PRIMERO y entero docs/encargos-fase3/00-comun.txt (contexto común: app iOS de Trajet, Swift 6 estricto sin Mac, estilo). Luego C:\Users\Isma\Desktop\Trajet v2\docs\app-v2-api.md (API interna del núcleo: modelos, TrajetAPI, stores, AppServices, PreviewData, demo; léete también el código real que uses en Trajet/Shared y Trajet/App). Otros agentes ya han escrito (o están integrando) el tablero y la navegación (Trajet/App/Views/Board, MainTabView, Views/Common), el mapa y el modo trayecto (Trajet/App/Views/Map, Trajet/App/Trip: `services.trip: TripController`, `services.tripSettings: TripSettings` con `maxMinutes`, `geofencesEnabled`, `watchedStationIDs` y `func requestAlwaysForGeofences() async` en el TripController o el servicio de geocercas — compruébalo en el código) y los widgets y la Live Activity (TrajetWidgets, Trajet/Shared/Activity, Trajet/Shared/Components). REUTILIZA sus componentes (LineBadge, piezas de Views/Common y Shared/Components) en vez de duplicarlos.
En esta etapa NO compilas ni haces push: un agente de integración compila todo en CI. Código especialmente cuidadoso.

TU PARTE (B3): RUTAS, PLANIFICADOR, ESTADÍSTICAS, AJUSTES y EMPAREJAMIENTO. Ficheros tuyos: Trajet/App/Views/Routes/* (RoutesScreen, PlannerScreen y los que crees: RouteEditorView, LegBuilderView, PlaceSearchField…), Trajet/App/Views/Stats/*, Trajet/App/Views/Settings/*, Trajet/App/Views/Pairing/* (PairingFlowView, QRScannerView con AVFoundation, ManualPairingView), y tests de lógica en TrajetTests/RoutesLogicTests.swift, PairingViewLogicTests.swift, StatsLogicTests.swift. Sustituye los stubs (mismos nombres de tipo, sin parámetros en el init público: RoutesScreen(), PlannerScreen(), StatsScreen(), SettingsScreen(), PairingFlowView()).
Aspecto: docs/diseno/sistema.md (§7.15 listas y formularios, §7.16 estadísticas, §7.17 emparejamiento, §7.13 hojas, §7.18 estados; y la capa Trajet/Shared/Design) y el prototipo design-lab/b-cristal (pantallas de rutas, editor, planificador, historial, ajustes y emparejamiento en app.js/style.css). Funcionalidad: docs/inventario-funcional.md §3–§8 (F de rutas, editor, montar un tramo, buscar/planificador, historial, ajustes): NO se pierde ninguna.
- RUTAS: lista con la ruta que toca marcada y el horario tal como se definió (R35, días en español con 0 = lunes, R36), borrar (con confirmación), editar, reordenar si la API lo permite (position), crear a mano y desde el planificador. Editor manual: nombre, origen/destino, días, modo de horario (franja / salgo a / llego a — R34, R38), tramos: buscar parada (mínimo 2 caracteres y 350 ms de espera —R31—), elegir línea (llegan ordenadas por modo; la app no reordena —R62—), elegir sentido de los que circulan ahora (/stops/{id}/directions; sin elegir = todos, y se avisa —R32—). Al guardar, `routes.create/update` y el tablero se refresca (R39).
- PLANIFICADOR: buscar lugar de origen y destino (paradas, direcciones y sitios; debounce 350 ms), cuándo (ahora / salir a / llegar a; `when` HH:MM y `mode` departure/arrival —R34—), opciones con duración, transbordos, caminar y líneas, y «Guardar como ruta» (nombre, días) con el aviso de los tramos que se quedan sin sentido (`without_direction`, R33).
- ESTADÍSTICAS: qué línea falla más (by_line), días con incidencias por mes (by_month; «n» son observaciones, no días), resumen, y el ACIERTO de la previsión de vía SOLO de /api/v1/platform-model (R11: la app nunca pregunta al usuario si acertó), con la cobertura por tramo. Gráficos con Swift Charts (iOS 16+), accesibles y con la paleta.
- AJUSTES: servidor y emparejamiento (nombre, direcciones LAN y Tailscale editables —se guardan al escribir, R61—, «Restablecer», probar conexión con ping, cuál responde, este dispositivo, volver a emparejar, desemparejar con confirmación); estado del servidor y cuota por endpoint (HealthStore: clave PRIM en uso sin enseñarla —estado y origen—, cuota con niveles, colector, traductor —R46—); modo trayecto (duración máxima, geocercas y estaciones vigiladas — TripSettings); permisos (ubicación, cámara, red local: estado y botón a Ajustes del sistema con UIApplication.openSettingsURLString — esto es de la app, no de la extensión); widgets y Live Activity disponibles o no (Capabilities: en lite o sin App Group, explicarlo con una frase); versión y build (CFBundleShortVersionString y CFBundleVersion); aviso de que con un Tailscale por IP 100.x la app necesita la excepción que ya lleva (y que con nombre MagicDNS *.ts.net también vale).
- EMPAREJAMIENTO (primera pantalla si no hay emparejamiento, docs/arquitectura.md §4, sistema.md §7.17): escanear el QR del panel con la cámara (AVFoundation: AVCaptureSession + AVCaptureMetadataOutput con .qr, en un UIViewControllerRepresentable o UIViewRepresentable; la sesión se arranca y para FUERA del hilo principal; permiso de cámara pedido con explicación y alternativa si se deniega), o meter a mano el código y las direcciones (LAN y/o Tailscale); estados conectando → emparejado (animación) / error diseñado (código no válido o caducado, demasiados intentos con el tiempo de espera, servidor no encontrado en ninguna dirección, ATS/red local); usa `services.pairing` (PairingStore: pair(link:), pair(code:lanURL:tailscaleURL:), phase, failure). El enlace trajet://pair abierto desde la cámara del sistema también llega aquí (AppServices.handle(url:) ya lo encamina).
- Accesibilidad (VoiceOver, 44 pt, Dynamic Type, reducir movimiento) y #Preview con PreviewData en #if DEBUG.
- Tests de la lógica que no sea vista (debounce/mínimo de caracteres R31, etiquetas de horario y días R35/R36, construcción del RouteInput desde el editor, parseo del código manual, textos de la cuota R46, acierto solo del platform-model R11).
Informe final: el que pide fase3-comun.txt.
