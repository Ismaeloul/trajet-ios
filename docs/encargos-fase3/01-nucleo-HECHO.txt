TU PARTE (FASE 3, etapa A): el NÚCLEO de la app, del que dependerán otros cuatro agentes que escribirán las vistas en paralelo después de ti. Tus nombres de tipos y firmas son el contrato para ellos: sé preciso y documenta en tu informe la API pública exacta.
Estado actual: el esqueleto compila en CI (project.yml, Trajet/App/TrajetApp.swift de esqueleto, TrajetWidgets/TrajetWidgetsBundle.swift de esqueleto, tests de esqueleto). Los modelos viejos están en Trajet/Shared/Model (Board, Routes, Plan, Stats, Decoding, PreviewData — este último dentro de #if DEBUG). La red y los stores viejos se borraron: están en `git show main:Trajet/Net/TrajetAPI.swift`, `…/ServerConfig.swift`, `…/Store/BoardStore.swift`, `…/Store/RoutesStore.swift` (léelos: tienen decisiones buenas —dos direcciones, reintento, caché en disco con hora de llegada— que hay que conservar, pero rehazlos para Swift 6 estricto y la API v1).

Escribe:
1. Trajet/Shared/Model/ (compila también en la extensión):
   - Actualiza Board.swift a BoardV1: `server: ServerState?`, `disruptionsOK: Bool` (true si falta), y en cada tramo `fromId: String`, `toId: String`, `platformExpected: Bool?` (si falta, dedúcelo del modo como hoy). Conserva la decodificación tolerante y lo que ya exista (Departure, LegStatus, Leg, TransportMode, BoardRoute, PlatformGuess, TrainLength…). Tipo para la respuesta del tablero: `enum BoardPayload: Sendable { case board(Board); case empty(message: String, server: ServerState?) }` con su decodificación.
   - Nuevos (ficheros aparte): `ServerState`, `ServerHealth` (HealthV1) con `QuotaSnapshot`/`QuotaEndpoint`/`PrimState` (+ collector, platform_model, translator de forma tolerante), `APIErrorBody` (ErrorV1: code, message, retry_after), `PingResponse`, `PairRequest` (Encodable), `PairResult`, `DeviceInfo`, `ServerURL`, `RouteMap` (MapLine, MapStation, MapPlatform, MapAccess, MapTransfer, MapLegStop…; ver esquema RouteMap del contrato) y `Polyline.decode(_ encoded: String) -> [CLLocationCoordinate2D]` (algoritmo de Google, precisión 5; importa CoreLocation).
   - Revisa Routes/Plan/Stats contra el contrato v1 (RouteList, Route, RouteInput, PlanOption, Alternatives, StatsV1, PlatformModel) y corrige lo que no case.
   - Amplía PreviewData (sigue en #if DEBUG) con TODOS los casos: los que ya tiene y los que faltan de docs/inventario-funcional.md F78/F79 (tablero viejo/sin conexión, stale, línea cortada con avisos y sin salidas, servidor sin clave (ErrorV1 prim_key_missing), cuota justa (server.refresh_hint_s 120), bus 106 y 165 min, vía que aparece, vía probable, tren en el andén, destinos mezclados, tramo vacío, 1 tramo y 6 tramos, transbordo, colores de línea raros (vacío, 3 dígitos, con #), health con y sin clave, RouteMap de la J Saint-Lazare → Argenteuil (puedes sacar polilínea y paradas de trajet-server/tests/fixtures/idfm o inventar una corta y plausible), emparejamiento…) con la forma EXACTA de la API v1. Da a cada caso un nombre claro (`PreviewData.board(.viaProbable)` o similar) y un JSON; R13.
2. Trajet/Shared/Cache/BoardCache.swift: guarda y carga el último tablero bueno con su hora de llegada al teléfono (R20) —JSON crudo + fecha—, en el contenedor del App Group si existe (lo leerán los widgets) o en Application Support si no. API: `struct CachedBoard: Codable, Sendable { let board: Board; let receivedAt: Date; let routeID: Int? }` y `enum BoardCache { static func load() -> CachedBoard?; static func save(_ c: CachedBoard) }` (lectura/escritura síncronas y seguras entre procesos: escritura atómica).
3. Trajet/Shared/Capabilities.swift: `enum Capabilities { static let appGroupID = "group.com.ismaeloul.trajet"; static var hasAppGroup: Bool; static var isLite: Bool (TRAJET_LITE); static var liveActivitiesAvailable: Bool }` (App Group comprobado en tiempo de ejecución con FileManager.containerURL(forSecurityApplicationGroupIdentifier:); Live Activities solo si no es lite, hay App Group y ActivityAuthorizationInfo().areActivitiesEnabled).
4. Trajet/Shared/Activity/TrajetActivityAttributes.swift: `struct TrajetActivityAttributes: ActivityAttributes` con su `ContentState`, EXACTAMENTE según docs/diseno/sistema.md §12.4 (solo campos que salen de la API o derivados documentados). Y Trajet/Shared/Activity/StopTripIntent.swift: `struct StopTripIntent: LiveActivityIntent` (título «Parar trayecto») cuyo perform() en la app llama a un manejador registrado por el modo trayecto: `@MainActor enum TripStopHandler { static var stop: (@MainActor () async -> Void)? }` (en la extensión, perform() no hace nada). Envuélvelo en `#if canImport(ActivityKit)` si hace falta y con disponibilidad iOS 17.
5. Trajet/App/Net/: `actor TrajetAPI` (dos direcciones en orden, recuerda la que responde; un error HTTP NO salta a la otra dirección y un fallo de red SÍ —R42—; timeouts 6 s / 12 s y sin caché HTTP —R43—; `Authorization: Bearer` con el token del Llavero; guarda ETag por URL y manda If-None-Match; 304 → devuelve lo guardado; decodifica fuera del MainActor; errores con `enum APIError: Error, Sendable { case notPaired, unauthorized, notFound, server(code: String, message: String, status: Int, retryAfter: Int?), network(String), decoding(String) }`). Métodos para TODAS las operaciones de /api/v1 del contrato (ping, pair, board(routeID:logHistory:), routes, route, createRoute, updateRoute, deleteRoute, routeFromPlan, routeMap, searchStops, searchPlaces, stopLines, stopDirections, plan, alternatives, stats, platformModel, health, me, unpair). `@MainActor @Observable final class ServerConfig` (direcciones LAN y Tailscale, preferida, nombre del servidor; persistidas en UserDefaults al escribir —R61—; `reset()`), `enum Keychain` (token del dispositivo: guardar, leer, borrar; kSecAttrAccessibleAfterFirstUnlock para que el modo trayecto funcione con el iPhone bloqueado).
6. Trajet/App/Store/: `@MainActor @Observable final class BoardStore` —pinta la caché al arrancar (< 1 s), bucle de refresco cada max(30, server.refreshHintS) s SOLO mientras la pantalla está delante (`setVisible(_:)`) o en modo trayecto (`setTripMode(_:)`) (R7); refrescos automáticos con log_history=false (R40); nunca borra la pantalla (R9): error → conserva el último tablero y lo dice; tramo con error y sin salidas → conserva sus últimas salidas buenas; errores con código → `issue` diseñado (sin clave, clave rechazada, cuota agotada, sin conexión, no emparejado); 404 de ruta fijada → vuelve a la automática (R44); antigüedad desde la llegada + data_age (R17); `isStale` = server stale o > 90 s sin tablero nuevo (R19, ver decisiones.md); elegir ruta refresca al momento (R39); la vía que aparece dispara un aviso para la háptica (`lastPlatformEvent`)—. `RoutesStore` (una sola copia y una sola carga —R41—, crear/editar/borrar/guardar desde plan), `HealthStore`, `MapStore` (mapa por ruta con ETag, en memoria + disco), `PairingStore` (parsea `trajet://pair?v=1&code=…&lan=…&ts=…&name=…`, valida, prueba las direcciones con ping y empareja; guarda token y direcciones; desemparejar).
   Y `@MainActor @Observable final class AppServices` que los reúne (`config, api, board, routes, health, maps, pairing`) con `static func live() -> AppServices` y, en DEBUG, `static func demo(escenario: String) -> AppServices`.
7. Trajet/App/Demo/DemoServer.swift (solo DEBUG): un `URLProtocol` que, si la app se lanza con el argumento `-demo` (y opcionalmente `-demoEscenario <nombre>`), responde a TODAS las peticiones de /api/v1 con PreviewData (emparejamiento con el código DEMO-2026, tablero según el escenario, rutas, mapa, salud, estadísticas, plan, alternativas). Lo usarán los tests de interfaz y las capturas.
8. Trajet/App/TrajetApp.swift y Trajet/App/RootView.swift: `@main` que crea AppServices (live o demo), lo inyecta con `.environment(...)`, maneja `.onOpenURL` (trajet://pair → PairingStore; trajet://route/<id> → BoardStore.selectRoute) y enseña `PairingFlowView` si no hay emparejamiento o `MainTabView` si lo hay. Crea STUBS mínimos (una vista con un Text, marcada `// STUB: lo sustituye <agente>`) de: `MainTabView`, `BoardScreen` (Trajet/App/Views/Board/), `MapScreen` (Views/Map/), `RoutesScreen` y `PlannerScreen` (Views/Routes/), `StatsScreen` (Views/Stats/), `SettingsScreen` (Views/Settings/) y `PairingFlowView` (Views/Pairing/), cada uno en su fichero, para que todo compile ya y los otros agentes solo tengan que reemplazar su fichero.
9. Tests (TrajetTests/, XCTest): decodificación tolerante de TODOS los PreviewData y de JSON raros (R21, R22), Polyline.decode, BoardCache (R20), TrajetAPI con un URLProtocol falso (orden de direcciones, error HTTP no salta, fallo de red sí, ETag/304, bearer, errores con código —R42, R43—), BoardStore (conserva el último tablero —R9—, conserva salidas por tramo, 404 vuelve a automática —R44—, refresco automático sin historial —R40—, bucle solo visible o en trayecto —R7—, stale —R19—, antigüedad —R17—), PairingStore (parseo del QR bueno y malo), ServerConfig (persistencia —R61—). Borra el EsqueletoTests.swift.
NO escribas las vistas de verdad (las hacen otros), ni la extensión de widgets, ni el modo trayecto (TripController), ni el mapa. Mantén el proyecto compilable: TrajetWidgets/TrajetWidgetsBundle.swift sigue siendo el esqueleto (no lo toques salvo que un cambio tuyo lo rompa).
AL TERMINAR, compila en CI tú mismo hasta que esté en verde (es tu parte): haz `git add` de tus ficheros, `git -c user.name=Isma -c user.email=ismaeloulhaji@gmail.com commit -m "<mensaje en español> [skip ci]"` (el mensaje termina con una línea en blanco y «Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>»), `git push`, y lanza la compilación con `gh workflow run ios.yml --ref rewrite-v2 -f modo=compilar`; espera con `gh run list -R Ismaeloul/trajet-ios -L 1` / `gh run view <id> -R Ismaeloul/trajet-ios --log-failed` (lee los errores `error:` y corrígelos) y repite. Cuando compile, lanza UNA vez `-f modo=completo` para que corran tus tests; si fallan, corrígelos. Sé austero: cada ejecución de CI cuesta minutos de macOS (x10 en repo privado); junta muchos arreglos por ejecución. Solo haces push a la rama rewrite-v2; nunca a main, nunca --force.
