Proyecto Trajet: app iOS nativa (SwiftUI) de transporte de Île-de-France que consume el servidor del usuario (Umbrel). Carpeta del repo: C:\Users\Isma\Desktop\Trajet v2 (repo trajet-ios, rama rewrite-v2). Escribe comentarios y textos de la interfaz en ESPAÑOL (tono cercano, frases cortas, como el código que ya hay).
LEE ANTES DE ESCRIBIR (lo que te toque, pero como mínimo):
- docs/app-v2.md (diseño técnico: targets, carpetas, principios; SÍGUELO),
- docs/openapi.yaml, sección /api/v1 y sus esquemas (BoardV1, BoardLegV1, Departure, LineStatus, PlatformGuess, ServerState, HealthV1, QuotaV1, PrimState, ErrorV1, Ping, PairRequest/PairResult, Device, RouteList/Route/RouteInput, RouteMap…): NO inventes campos que la API no tiene,
- docs/reglas.md (R1–R62 son de la app: cada una tendrá test),
- docs/decisiones.md (sobre todo «Notas que la FASE 3 (app) tiene que respetar»),
- docs/inventario-funcional.md (funcionalidades F1–F82 de la v1 que NO se pueden perder; el código viejo está en la rama main: `git show main:Trajet/Views/Board/BoardView.swift` etc.),
- docs/diseno/sistema.md (el sistema «Cristal»: tokens, componentes con estados, animaciones y cómo se traduce a SwiftUI) y la capa ya escrita en Trajet/Shared/Design/ (Tokens.swift con Palette y Tokens, Typography.swift, Metrics.swift, Motion.swift, Glass.swift con .cristal(...), LineColor.swift con LineBadge, Format.swift). USA esa capa, no inventes colores ni tamaños.
Estructura (project.yml, XcodeGen): Trajet/Shared/ se compila en la app Y en la extensión de widgets (TrajetWidgets); Trajet/App/ solo en las dos apps (Trajet = IPA full, TrajetLite = IPA lite con la condición de compilación TRAJET_LITE); TrajetWidgets/ solo en la extensión (condición TRAJET_WIDGETS); TrajetTests/ (XCTest, @testable import Trajet) y TrajetUITests/. En Shared NADA que no exista en una extensión (nada de UIApplication.shared, ni de APIs marcadas NS_EXTENSION_UNAVAILABLE).
COMPILACIÓN — MUY IMPORTANTE: en este PC NO hay Mac ni compilador de Swift; se compila en GitHub Actions (Xcode 26.6, SDK iOS 26.5). Escribe Swift que compile A LA PRIMERA:
- iOS 17 mínimo; todo lo de iOS 18/26 tras `if #available(iOS 26.0, *)` (o 18.0) y con alternativa.
- Swift 6 con SWIFT_STRICT_CONCURRENCY=complete y aislamiento por defecto clásico (NO MainActor por defecto): estado de UI en clases `@MainActor @Observable final class`; red y trabajo pesado en `actor`s; modelos `struct … : Sendable`; nada de estado global mutable (`static var` mutable está prohibido salvo `nonisolated(unsafe)` justificado); closures @Sendable donde toque; sin `DispatchQueue.main.async` (usa `Task { @MainActor in … }` o `await MainActor.run`).
- APIs conservadoras y conocidas; si dudas de una firma, usa la alternativa segura y deja `// TODO-COMPILAR: …`. Nada de dependencias externas.
- Todo `switch` exhaustivo, todos los `init` completos, `Codable` a mano cuando el modelo sea tolerante (patrón de Trajet/Shared/Model/Decoding.swift: c.get/c.opt/c.text).
- Evita nombres que choquen con los de la capa Design (Palette, Tokens, Metrics, Motion, CristalRole, LineBadge, Pace, Format…) o con otros agentes (usa los nombres de tipos que se te indican).
Reglas de trabajo: NO hagas commits ni push (salvo que tu parte lo diga). Solo edita/crea los ficheros de tu parte; si necesitas algo de otra parte, usa la interfaz indicada y, si falta, dilo en el informe. NO llames al servidor de producción (192.168.1.188 / 100.99.38.76) ni a PRIM. Nunca escribas claves ni tokens reales.
Informe final (tu respuesta): ficheros creados/cambiados, tipos públicos que exportas (con su firma) para que otros los usen, reglas R cubiertas y con qué test, dudas de compilación (TODO-COMPILAR) y lo que falte.
