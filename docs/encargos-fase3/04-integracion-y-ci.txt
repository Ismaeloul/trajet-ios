Lee PRIMERO y entero docs/encargos-fase3/00-comun.txt (contexto común de la app iOS de Trajet y reglas de Swift 6 estricto) y C:\Users\Isma\Desktop\Trajet v2\docs\app-v2-api.md.

TU PARTE: INTEGRACIÓN Y COMPILACIÓN EN CI de la app iOS (repo C:\Users\Isma\Desktop\Trajet v2, rama rewrite-v2). Varios agentes han escrito, SIN compilar, el tablero y la navegación (Trajet/App/Views/Board, Views/MainTabView.swift, Views/Common), el mapa y el modo trayecto (Trajet/App/Views/Map, Trajet/App/Trip, cambios en AppServices.swift) y los widgets y la Live Activity (TrajetWidgets/, Trajet/Shared/Activity, Trajet/Shared/Components, Trajet/Shared/Design/DestinationAbbreviator.swift, una línea en BoardStore.swift), con sus tests en TrajetTests/. Lee el código que dejaron y sus comentarios TODO-COMPILAR (y docs/encargos-fase3/informes-etapaB.md si existe).
A LA VEZ QUE TÚ, otro agente (B3) está escribiendo las pantallas de rutas, planificador, estadísticas, ajustes y emparejamiento en Trajet/App/Views/Routes, Trajet/App/Views/Stats, Trajet/App/Views/Settings y Trajet/App/Views/Pairing (y sus tests TrajetTests/RoutesLogicTests.swift, PairingViewLogicTests.swift, StatsLogicTests.swift). NO toques esas carpetas ni esos tests, y NO los incluyas en tus commits: haz siempre `git add -A -- . ':!Trajet/App/Views/Routes' ':!Trajet/App/Views/Stats' ':!Trajet/App/Views/Settings' ':!Trajet/App/Views/Pairing' ':!TrajetTests/RoutesLogicTests.swift' ':!TrajetTests/PairingViewLogicTests.swift' ':!TrajetTests/StatsLogicTests.swift' ':!PROMPT-*'` (así CI compila con los stubs que ya están en git para esas pantallas). Si algo tuyo necesita algo de esas pantallas, usa solo los nombres de los stubs.
Tu trabajo:
1. Revisa que las piezas encajan entre sí (nombres, firmas, AppServices con trip/tripSettings, ActivityContentBuilder, WidgetRefresher, DestinationAbbreviator, RouteMapView, TripController) y que nada de Trajet/Shared usa APIs de la app (la extensión compila Shared + TrajetWidgets). Arregla lo que no case con el cambio MÍNIMO y sin cambiar el diseño.
2. Compila en CI hasta que TODO compile: `git add -A` (sin los PROMPT-*.md de la raíz), commit con mensaje en español terminado en «[skip ci]» y la línea «Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>» (usa `git -c user.name=Isma -c user.email=ismaeloulhaji@gmail.com commit`), `git push` (solo rewrite-v2; nunca main, nunca --force), `gh workflow run ios.yml -R Ismaeloul/trajet-ios --ref rewrite-v2 -f modo=compilar`, espera a que acabe (`gh run list -R Ismaeloul/trajet-ios -L 1`, luego `gh run view <id> -R Ismaeloul/trajet-ios --log-failed` o `--log` y grep "error:") y corrige TODOS los errores de una tanda antes de volver a lanzar. El repo ya es público (CI gratis), pero junta arreglos por ejecución para ir más rápido. Mira también los avisos de concurrencia (warnings de Sendable/actor) cuando compile: arréglalos si son reales.
3. Cuando compile, lanza `-f modo=completo` (IPA full y lite + tests unitarios y de interfaz). Si fallan tests, arregla el código o el test (si el test está mal) y repite. Objetivo: verde en «completo».
4. No borres funcionalidad para que compile: si algo es irrecuperable sin rediseño, déjalo compilando con la alternativa más fiel y apúntalo en el informe.
Informe final: número de ejecuciones de CI y la última en verde (id), qué tuviste que cambiar (fichero y por qué), avisos que queden, y cualquier cosa que no funcione todavía.
