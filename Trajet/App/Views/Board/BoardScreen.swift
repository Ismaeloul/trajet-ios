import SwiftUI

// STUB: lo sustituye el agente del tablero.
// Tiene que llamar a `services.board.setVisible(true/false)` al aparecer y al
// irse (R7).
struct BoardScreen: View {
    var body: some View {
        Text("Tablero")
    }
}
