import SwiftUI

// STUB: lo sustituye el agente del emparejamiento.
// Usa `services.pairing` (handle(url:), pair(link:), pair(code:lanURL:tailscaleURL:)).
struct PairingFlowView: View {
    var body: some View {
        Text("Trajet")
    }
}
