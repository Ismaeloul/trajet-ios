import SwiftUI

// STUB: lo sustituye el agente del tablero y la navegación.
// Tiene que leer `services.pendingLink` (consumePendingLink()) para abrir la
// pestaña o la hoja que pida un enlace trajet://.
struct MainTabView: View {
    var body: some View {
        Text("Trajet")
    }
}
